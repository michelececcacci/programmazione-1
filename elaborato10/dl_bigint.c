#include <stdio.h>
#include "selector.h"
#ifdef DL_BIGINT
#include <stdlib.h>
#include "dl_bigint.h"

void reverse_doubly(bigint *L);
static long int value_from_list(bigint * L);

bigint *mul(bigint *N1, bigint *N2) {
    return NULL;
}


#endif
